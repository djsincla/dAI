services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: dai
      POSTGRES_PASSWORD: dai
      POSTGRES_DB: dai
    ports: ["5433:5432"]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dai"]
      interval: 2s
      timeout: 3s
      retries: 20
